export type Filter = {
  shape: Array<string>;
  color: Array<string>;
  size: Array<string>;
  year: Array<number>;
  count: Array<number>;
};

export type Data = {
  num: string;
  name: string;
  count: string;
  year: string;
  shape: string;
  color: string;
  size: string;
  favorite: boolean;
};
